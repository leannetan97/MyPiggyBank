import 'dart:ui';

class ColorsUtil {
  // Hex: #8A2BE2
  static const Color blueViolet = Color.fromRGBO(54, 17, 89, 1);

  // Hex: #6495ED
  static const Color cornFlowerBlue = Color.fromRGBO(39, 58, 93, 1);

  // Hex: #63dff2
  static const Color lightBlue = Color.fromRGBO(99, 223, 242, 1);

  // Hex: #803aa1
  static const Color darkPurple = Color.fromRGBO(128, 58, 161, 1);

  // Hex: #4db6ac
  static const Color green = Color.fromRGBO(77, 182, 172, 1);

  // Hex: #000000
  static const Color black = Color.fromRGBO(0, 0, 0, 1);

  // Hex: #FFFFFF
  static const Color white = Color.fromRGBO(255, 255, 255, 1);
}
